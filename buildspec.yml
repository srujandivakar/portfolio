version: 0.2

phases:
  install:
    commands:
      - echo "-------"

  pre_build:
    commands:
      - echo "------"

  build:
    commands:
      - echo "--------"

  post_build:
    commands:
      - echo "Build completed."

artifacts:
  files:
    - '**/*'