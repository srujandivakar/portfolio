version: 0.0
os: linux

files:
  - source: /
    destination: /usr/share/nginx/html

hooks:

  BeforeInstall:

    - location: scripts/cleanup.sh

      timeout: 300

      runas: root

file_exists_behavior: OVERWRITE